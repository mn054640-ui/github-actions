name: exercise-1

on: 
    push:
        branches:
            - main

jobs:
    learning:
        runs-on: ubuntu-latest
        steps:
            - name: learning
              run: "echo Nadeem is a Devops Engineer"
            - name: list env
              run: printenv