name: exercise-4

on:
    pull_request:
        branches:
            - dev

jobs:
    exercise-4:
        runs-on: ubuntu-latest
        steps:
            - name: message
              run: echo "Thanks for me pull request"