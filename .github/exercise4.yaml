---
on:
    pull_request:
        branches: 
            - dev

jobs:
    exercise4:
        runs-on: ubuntu-latest
        steps:
            - name: learning
              run: "echo thanks for pull request" 
            - name: message
              run: printenv